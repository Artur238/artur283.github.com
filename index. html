<!DOCTYPE html>
<html>
<head>
    <title>Загрузка...</title>
    <meta charset="utf-8">
</head>
<body>
    <p>Загрузка начнется через секунду. Если нет, <a id="downloadLink" href="ВАШ_ФАЙЛ.exe">нажмите сюда</a>.</p>

    <script>
        // Функция для автоматического скачивания
        window.onload = function() {
            // Укажите точное название вашего файла вместо 'ВАШ_ФАЙЛ.exe'
            var fileName = 'file.txt'; 
            
            var link = document.createElement('a');
            link.href = fileName;
            link.download = fileName;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        };
    </script>
</body>
</html>
